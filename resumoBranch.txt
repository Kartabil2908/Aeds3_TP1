🎁 Resumo do Projeto: PresenteFácil 1.0
==================================================

✨ Funcionalidades Principais
--------------------------------------------------

* Gestão Completa de Utilizadores:
    - Criar, ler, atualizar e apagar utilizadores.

* Autenticação:
    - Sistema de login com verificação de e-mail e palavra-passe.

* Dois Tipos de Exclusão:
    1. Desativar Conta (Soft Delete):
        - Marca o utilizador como "inativo" mas mantém os dados.
        - O utilizador desativado não pode fazer login.
    2. Excluir Conta (Hard Delete):
        - Apaga permanentemente os dados do utilizador.
        - Liberta o espaço para ser reutilizado.


🛠️ Arquitetura Técnica
--------------------------------------------------

* Persistência em baixo nível:

    - Ficheiros Binários:
        - Todos os dados são guardados em arquivos `.db` na pasta `data/usuarios`.
        - O ficheiro principal é `usuarios.db`.

    - Indexação com Hash Extensível:
        - Índice Primário: busca rápida por `ID`.
        - Índice Secundário: busca por `e-mail` (essencial para login).

====================================================================================================================


🎁 Análise Detalhada do Projeto: PresenteFácil 1.0
==================================================

1. Fluxo de uma Operação: A Jornada de um Login
--------------------------------------------------

1. Interface (UI Layer):
    - Terminal captura e-mail e palavra-passe.
    - Invoca o `ControladorUsuario`.

2. Lógica de Negócio:
    - `ControladorUsuario` chama `arqUsuarios.read(email)`.

3. Acesso a Dados:
    a. `ArquivoUsuario` consulta o Índice Secundário por E-mail.
    b. Calcula o hash e acessa `indiceIndiretoEmail`.
    c. Retorna o `ID` correspondente.
    d. Com o `ID`, chama `super.read(id)`.
    e. A classe `Arquivo` consulta o Índice Primário.
    f. Localiza endereço em bytes nos ficheiros `usuarios.d.db` e `usuarios.c.db`.
    g. Usa `seek(endereco)` para acessar direto o registo.
    h. Desserializa os dados e reconstrói o objeto `Usuario`.

4. Conclusão:
    - Verifica hash da password e se o campo `ativo` é `true`.

5. Feedback:
    - Resultado do login é retornado ao terminal.


2. Análise Profunda da Camada de Persistência
--------------------------------------------------

2.1. Ficheiro de Dados Principal (`usuarios.db`)
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

* Estrutura Binária:
    - Cabeçalho (12 bytes):
        - último ID (4 bytes)
        - ponteiro da lista de livres (8 bytes)

    - Registo:
        - Lápide (1 byte): `' '` (ativo) ou `'*'` (apagado)
        - Tamanho do registo (2 bytes)
        - Dados serializados (n bytes)

* Gestão de Espaço:
    - `delete`: marca como excluído e insere em lista de espaços livres.
    - `create`: reutiliza espaço livre, se possível.

2.2. Indexação: A Magia do Hash Extensível
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

* Estrutura:
    - Diretório `.d.db`: array de ponteiros (2^pG entradas)
    - Cestos `.c.db`: blocos com dados (ex: `ParEmailID`)

* Inserção:
    1. Calcula hash da chave.
    2. Usa `pG` bits para encontrar entrada no diretório.
    3. Localiza e atualiza cesto no ficheiro.

* Cesto Cheio:
    - Divide o cesto.
    - Redistribui elementos.
    - Atualiza ponteiros no diretório.
    - Se necessário, duplica diretório (aumenta pG).

* Índices:
    - Primário: `ParIDEndereco (ID -> Endereço)`
    - Secundário: `ParEmailID (hash(E-mail) -> ID)`


3. Conclusão Final
--------------------------------------------------

- Acesso aleatório a ficheiros
- Serialização de objetos
- Reaproveitamento de espaço e controle de fragmentação
- Indexação primária e secundária
- Hash extensível persistente
- Arquitetura multi-camada

